# ==============================================================================
# HumanEgo — Core Dependencies
# ==============================================================================
# Install with: pip install -r requirements.txt
# For full setup (including hand tracking), use: SKIP_HAND=0 bash setup.sh
# ==============================================================================

# ── PyTorch — pinned to a CUDA 12.x build (works on driver >= 525 / CUDA 12.x) ──
# NOTE: unpinned `torch>=2.5` resolves to the newest cu13x wheel, which fails on the
# common CUDA 12.x drivers (torch.cuda.is_available() == False). Newer GPUs (e.g.
# Blackwell) may need a newer cu12x/cu13x build — adjust the version + index to match
# your driver (see https://pytorch.org). Verify after install:
#   python -c "import torch; print(torch.cuda.is_available())"
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
xformers>=0.0.28

# ── Scientific Computing ──
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.21
pandas>=2.0

# ── Computer Vision ──
opencv-python>=4.8
pillow>=10.0
open3d>=0.18
imageio>=2.30
imageio-ffmpeg>=0.5
pyrender>=0.1.45
trimesh>=4.0

# ── Deep Learning Models ──
transformers>=4.40,<5    # 5.x breaks Grounding DINO's AutoProcessor (preprocess dinosam stage)
sam2>=1.0
diffusers>=0.25
timm>=0.9
einops>=0.7
rembg>=2.0
onnxruntime-gpu>=1.17
ultralytics>=8.3
filterpy>=1.4
ffmpeg-python>=0.2

# ── 3D / Pose ──
roma>=1.4
pyquaternion>=0.9
smplx>=0.1.28

# ── Project Aria & Robot Hardware ──
# NOTE: projectaria-client-sdk 1.1.0 hard-pins projectaria-tools==1.5.2a1, but the
# pipeline needs 1.7.1 — setup.sh installs both with --no-deps, so pip prints a
# (harmless) resolver-conflict warning about that pin. termcolor is a client-sdk CLI
# dependency; listing it here silences the "termcolor is not installed" half.
projectaria-tools==1.7.1
projectaria-client-sdk==1.1.0
termcolor>=2.0
pyrealsense2>=2.50
trossen-arm>=1.8

# ── Configuration ──
omegaconf>=2.3
hydra-core>=1.3
pyyaml>=6.0
python-box>=7.0
pydantic>=2.0

# ── ML Ops & Data ──
huggingface-hub>=0.23
wandb>=0.16
accelerate>=0.25

# ── Utilities ──
tqdm>=4.65
rich>=13.0
python-dotenv>=1.0
requests>=2.31
safetensors>=0.4

# ── Visualization ──
matplotlib>=3.7
seaborn>=0.13
plotly>=5.15
rerun-sdk>=0.18

# ── Web / Demo ──
gradio>=5.0