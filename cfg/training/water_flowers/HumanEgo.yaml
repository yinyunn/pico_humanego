# HumanEgo training config for the `water_flowers` task.
# Loads the released water_flowers dataset (data_sources: aria) from
# ./data/water_flowers/aria/mps_water_flowers_*_vrs.

# ---- Optimization & Schedule ----
epochs: 400
batch_size: 32
lr: 1.0e-4
weight_decay: 1.0e-2
grad_clip: 1.0
use_lr_schedule: True
warmup_steps: 200
min_lr_ratio: 0.05
use_amp: True

# ---- EMA ----
use_ema: True
ema_decay: 0.999

# ---- Model Hyperparams ----
num_inference_steps: 20
model_h_weighting: "uniform"
model_h_beta: 0.0
patch_size: 16
vision_embed_dim: 384
num_decoder_layers: 6
num_heads: 8
mlp_ratio: 4.0
dropout: 0.05

# ---- Evaluation ----
eval_every: 1
vis_eval_every: 100

# ---- Data & Task ----
single_hand: False                 # water_flowers is bimanual
hand_tracking_method: "aria_mps"
data_sources:
  aria: 60
eval_source: "aria"
num_workers: 8

# ---- Augmentations ----
use_legacy_rng: True
enable_augmentation: True
enable_aug_img: True
enable_aug_rrc: True
enable_aug_target_jittering: True
enable_aug_cutout: True
enable_aug_temporal_stride: False
enable_aug_interpolation: True

# ---- Paradigm Ablations ----
pred_horizon: 50
img_name: "rgb_WoArm_WArmObjKpts.png"
image_size: [240, 320]
centric_mode: "object_centric"
frame_mode: "anchor_frame"
action_mode: "absolute"
max_ict: 8
use_region_attn: False
use_ot_cfm: False

# ---- Loss Weights ----
w_flow: 1.0
w_pos: 5.0
w_rot: 1.0
w_g: 10.0
w_done: 0.2
use_aux_obj_dynamics: False
use_aux_visual_foresight: False
w_foresight: 20.0
use_aux_temporal_contrastive: False
w_contrastive: 0.1

# ---- Legacy Compatibility ----
use_pre_norm: False
use_ctx_norm: False
use_done_in_flow: False
use_legacy_image_loading: True
