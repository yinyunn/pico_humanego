# 历史记录：serve_bread 的 exp_0000

此前曾针对 `serve_bread` 完成一次基线冒烟测试，结果为 `SUCCESS`，固定分数为
`1.5743332073`。任务现已切换为 `open_door`，该历史结果不属于当前任务，未被
纳入新的 `results.tsv` 或 leaderboard。对应运行产物仍保留在被忽略的
`runs/serve_bread/autoresearch/exp_0000/` 中。
